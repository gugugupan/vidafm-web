<% content_for :title do %>
排行榜&获奖名单
<% end %>

<!--排行榜&获奖名单-->
<div class="container wrapper">
    <%= render :partial => "nav", :locals => { :user => @cur_user } %>

    <header>
        <div class="row-fluid">
            <div class="span12">
                <img src="/assets/weiboactive/banner-award.png">
            </div>
        </div>
    </header>

    <%= render :partial => "daystar" %>

    <div class="row-fluid">
        <div class="span12">
            <div class="text-center block-title">
                排行榜&获奖名单
            </div>
        </div>
    </div>
    <% unless @mobile == true %>

    <div class="row-fluid">
        <div class="span6">
            <div class="placard">
                <div class="title">
                    <%= @title %> 今日排行榜
                </div>
                <ul class="talent-container">
                    <%= render :partial => "widgets/award_item" , :collection => talentUserJson(@noAwardSortJson), :locals => { :show_type => @showType} -%>
                </ul>
                <br>
                <div class="pagination pagination-large" href="/weiboactive/gettopuser" data-toggle="pagination" data-total-page="<%= @noAwardPage %>" data-current-page="1" data-type="<%= @showType %>" data-type-num="0">
                    <ul>
                        <li class="active">
                            <a href="#">1</a>
                        </li>
                        <li>
                            <% unless @noAwardPage == 1 || @noAwardPage == 0 %>
                            <a href="#" data-to-page="2">下一页</a>
                            <% else %>
                            <span>下一页</span>
                            <% end %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="span6">
            <div class="placard">

                <div class="title">
                    <%= @title %> 获奖名单
                </div>
                <ul class="talent-container">
                    <%= render :partial => "widgets/award_item" , :collection => talentUserJson(@awardSortJson), :locals => { :show_type => "award"} -%>
                </ul>
                <br>
                <div class="pagination pagination-large" href="/weiboactive/gettopuser" data-toggle="pagination" data-total-page="<%= @awardPage %>" data-current-page="1" data-type="<%= @showType %>" data-type-num="1">
                    <ul>
                        <li class="active">
                            <a href="#">1</a>
                        </li>
                        <li>
                            <% unless @awardPage == 1 || @awardPage == 0 %>
                            <a href="#" data-to-page="2">下一页</a>
                            <% else %>
                            <span>下一页</span>
                            <% end %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <% else %>
    <div class="row-fluid">
        <div class="span12">
            <div class="placard">
                <div class="title">
                    <%= @title %> 今日排行榜
                </div>
                <ul class="talent-container">
                    <%= render :partial => "widgets/award_item" , :collection => talentUserJson(@noAwardSortJson), :locals => { :show_type => @showType} -%>
                </ul>
                <br>
                <div class="pagination pagination-large" href="/weiboactive/gettopuser" data-toggle="pagination" data-total-page="<%= @noAwardPage %>" data-current-page="1" data-type="<%= @showType %>" data-type-num="0">
                    <ul>
                        <li class="active">
                            <a href="#">1</a>
                        </li>
                        <li>
                            <% unless @noAwardPage == 1 || @noAwardPage == 0 %>
                            <a href="#" data-to-page="2">下一页</a>
                            <% else %>
                            <span>下一页</span>
                            <% end %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="row-fluid">
            <div class="span12">
                <div class="placard">

                    <div class="title">
                        <%= @title %> 获奖名单
                    </div>
                    <ul class="talent-container">
                        <%= render :partial => "widgets/award_item" , :collection => talentUserJson(@awardSortJson), :locals => { :show_type => "award"} -%>
                    </ul>
                    <br>
                <div class="pagination pagination-large" href="/weiboactive/gettopuser" data-toggle="pagination" data-total-page="<%= @awardPage %>" data-current-page="1" data-type="<%= @showType %>" data-type-num="1">
                    <ul>
                        <li class="active">
                            <a href="#">1</a>
                        </li>
                        <li>
                            <% unless @awardPage == 1 || @awardPage == 0 %>
                            <a href="#" data-to-page="2">下一页</a>
                            <% else %>
                            <span>下一页</span>
                            <% end %>
                        </li>
                    </ul>
                </div>
                </div>
            </div>
        </div>

        <% end %>

        <%= render :partial => "footer" %>
    </div>
